# Rayact

The consumer umbrella for Rayact. React APIs are available from `rayact` or `rayact/react`; built-in KV, crypto, and worker APIs use `rayact/kv`, `rayact/crypto`, and `rayact/worker`. Optional native features are separate packages.
