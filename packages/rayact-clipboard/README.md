# @rayact/clipboard

Promise-based text clipboard access for Rayact. Importing the package also
installs compatible `navigator.clipboard.readText()` and `writeText()` methods
when the host does not already provide them.
