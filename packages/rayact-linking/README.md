# @rayact/linking

Raw URL transport for Rayact applications. Configure custom schemes with
`linking.schemes` in `rayact.config.json`. A future `@rayact/router` can consume
these events, but this package intentionally owns no route state.
