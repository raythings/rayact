# Changelog

## 0.0.5

- About page falls back to the project's configured Android package id / iOS
  bundle id, `package.json` version, and installed `@rayact/*` version when
  native `getAppInfo` is unavailable.
- About no longer shows placeholder dashes for those fields on custom clients.

## 0.0.3

- First standalone package release.
