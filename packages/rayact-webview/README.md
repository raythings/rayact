# @rayact/webview

Compatibility-oriented WebView for Rayact. Android uses the engine platform-view
texture path. iOS uses `WKWebView` composition. Web/desktop support is
experimental and reports unavailable when the host has no platform-view backend.
