# @rayact/prebuilt-web-wasm

Rayact `0.0.3` wasm32/WebGPU host. Serve it in a secure, cross-origin-isolated context with COOP/COEP. The host contains built-in KV, crypto, and worker support, but no optional native storage modules.
