# @rayact/prebuilt-android-x64

Rayact `0.0.3` engine libraries for Android x86_64. This package contains only the generic engine and C++ runtime. Optional native modules are delivered by their own packages.
